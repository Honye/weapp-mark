<!-- 喜欢的影单 -->
<view class='container page-favlist'>
  <block wx:for='{{list}}' wx:key='id'>
    <com-article
      aid='{{item.id}}'
      image='{{item.image}}'
      title='{{item.title}}'
      like-count='{{item.likeCount}}'
    />
  </block>
</view>
